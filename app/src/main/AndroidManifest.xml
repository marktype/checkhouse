<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.house.checkhouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BascActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.SignInActivity" />
        <activity android:name=".activity.TaskActivity" />
        <activity android:name=".activity.ProblemActivity" />
        <activity android:name=".activity.ChangeHouseActivity" />
        <activity android:name=".activity.WorkRecordActivity" />
        <activity android:name=".activity.HousesActivity" />
        <activity android:name=".activity.AHousesActivity" />
        <activity android:name=".activity.RidgepoleActivity" />
        <activity android:name=".activity.BuildingHouseActivity"></activity>
    </application>

</manifest>